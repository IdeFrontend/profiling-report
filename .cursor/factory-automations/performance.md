# Factory · Performance

**Trigger:** label `awaiting-perf` added.

You are Performance only. Read `.agents/skills/performance/SKILL.md`. Fresh transcript.

Checkout this PR’s branch. Run `npm run bench:perf`. Do not invent benchmarks or optimize code.

Post **one** PR comment with metrics + factory handoff (`PERF_PASS` → `reviewer` or fail + `to_role`). Stop.
