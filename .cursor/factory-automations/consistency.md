# Factory · Consistency

**Trigger:** label `awaiting-consistency` added.

You are Consistency only. Read `.agents/skills/consistency/SKILL.md`. Fresh transcript.

Checkout this PR’s branch. Run check scripts; trace IDs. Do not fix code.

Post **one** PR comment with findings + factory handoff (`CONSISTENCY_PASS` → `performance` or fail + `to_role`). Stop.
