请分析需求并完成需求开发，需求详情如下，

1. 在报告统计页面，需要增加调用cannbot插件的图标入口
   - 按钮具体位置如下（也称章节）：
     - 位置1（章节1）：在“进程: 3073000 算子类型: mix Blocks: 8 更多”的右边增加一个图标入口（按钮水平居右对其，与左侧文字垂直居中对其）
     - 位置2（章节2）：在“计算负载分析”模块中，“详情”按钮的左边增加一个图标入口（与右侧文字垂直居中对其）
     - 位置3（章节3）：在“内存负载分析”模块中，“详情”按钮的左边增加一个图标入口（与右侧文字垂直居中对其）
2. 点击图标入口，本项目主要做一件事情：数据组装，给上层调用方传递组装好的数据
   - 目标数据格式为：
       {
           "version": "1.0",
           "scope": "roofline | sol | pipeline| compute | memory | communication",
           "report_name": "matmul_v3.r3",
           "report_id": "XXXX",
           "collected_at": "2026-08-13T09:41:00Z",
           "data": { /* scope 对应字段 */ }
           "prompt": "XXXX",
       }
   - 数据说明：
     - version：固定为“1.0”
     - scope：字符串，用于区分当前报告章节，支持枚举值：compute（章节2）、memory（章节3）、summary（章节1）
     - report_name：“.rep”文件名称
     - report_path：“.rep”文件绝对路径
     - op_name：当前报告的算子名称
     - collected_at：当前报告的采集时间
     - data：章节数据，JSON格式，eg：{“key1”: “value1”, “key2”: “value2”}。**这里重点说明下**：章节中所有展示的数据（包括点击详情后展示的数据）应该都是先解压.rep后再从.csv文件中读取的。我们这个需求禁止自己解析数据，而是要复用现有的、已经解析好的数据，这样可以保证逻辑单一，易于维护。
     - prompt：固定是下方“prompt”所示的文本内容

**prompt**：
```text
【你能看到的数据边界（必须严格遵守）】
除 payload JSON 外，你看不到任何报告数据、
CSV原始文件、算子源码全文、其他报告或历史基线。
这是只读快照，不是实时查询接口，无法在本轮
对话里被补充或刷新。
【没有写权限，也没有跨报告数据访问能力】
如果问题满足以下任一条件，明确告知超出当前问答
模式的能力，需要更完整的工具访问能力（MCP通道）：

涉及"和上次/历史/基线比"这类跨报告对比
涉及"帮我改/优化/执行"这类写操作
涉及 payload.data 里明确没覆盖的细粒度数据
不要猜测、不要用近似值代替，也不要说
"我帮你查一下"之类暗示自己能查到的话。
```

