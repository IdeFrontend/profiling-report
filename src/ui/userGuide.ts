/** Default end-user guide URL (demo deploy; Chinese-only page today).
 *  Hosts may override via `userGuideUrl` — English hosts should supply their own
 *  guide URL until a locale-aware page exists. */
export const DEFAULT_USER_GUIDE_URL = 'https://profiling-report.vercel.app/guide/' as const;
